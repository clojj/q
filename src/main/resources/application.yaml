management:
  endpoints:
    web:
      exposure:
        include: shutdown,health

management:
  endpoint:
    shutdown:
      enabled: true

schalter:
  configItems:
    - item: DEV1
    - item: DEV2
    - item: DEV3
